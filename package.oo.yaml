version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  {}
name: aws-s3
repository: https://github.com/oomol-flows/oomol-aws-s3
icon: ":simple-icons:amazonwebservices:currentColor:"
description: Provides S3 object operations such as upload, delete, and download
  based on AWS S3 SDK
displayName: AWS S3
is_template: true
